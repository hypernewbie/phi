/phi
*.exe
*.exe~
*.db
*.pb
sessions.json
config.json
temp/
/webdist/
coverage/
*.out
node_modules/

# TypeScript build cache (one per tsconfig.build.json invocation).
*.tsbuildinfo

# pi-subagents scratch/artifacts
.pi-subagents/

# Local-only agent memory file. Holds working rules + repeated
# reminders for AI agents working on phi. Never enters the repo.
AGENTS.md
